#include<stdio.h>
#include<math.h>
int main(){

    // Interest I = Principal P * Rate R * Time T.

    double prinipal = 23, rate = 5, time = 2;
    double interest = prinipal*rate*time;

    printf("Simple Interest : %.2lf", interest);
}