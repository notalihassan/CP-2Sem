#include<stdio.h>

int main(){

    // Area of Triangle = 1/2 * base * height;

    double base = 3, height = 5, area;

    area = 0.5 * base * height;

    printf("Area of Triangle = %lf", area);

    return 0;
}